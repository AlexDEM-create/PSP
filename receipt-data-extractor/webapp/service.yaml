apiVersion: v1
kind: Service
metadata:
  name: receipt-data-extractor-service
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: receipt-data-extractor
